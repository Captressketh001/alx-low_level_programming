#include "main.h"

/**
 * add - a function that adds two integers and returns the result.
 * @x: integer to expect 1
 * @y: integer to expect 2
 * Return: the result
 */
int add(int x, int y)
{
	return (x + y);
}
