#include "main.h"
#include <string.h>

/**
 * _strlen - a function that returns the length of a string
 * @s: pointer to the variable
 * Return: lenght
 */

int _strlen(char *s)
{
	return (strlen(s));
}
